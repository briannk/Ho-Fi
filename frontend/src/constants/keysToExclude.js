// keys that are not relevant for diplay but still needed for
// other actions
const KEYS_TO_EXCLUDE = {
  expenses: ["id", "total", "description"],
  income: ["id", "total", "description"],
};

export default KEYS_TO_EXCLUDE;
